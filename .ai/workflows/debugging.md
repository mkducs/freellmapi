# Debugging Workflow

Failure → Reproduce → Observe → Hypotheses → Experiments → Root Cause → Minimal Fix → Regression Test → Verify

A successful fix requires evidence that the original failure is resolved and no material regression was introduced.
