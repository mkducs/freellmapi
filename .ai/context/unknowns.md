# Unknowns

Open questions that could not be established from the repository. Each needs a
human answer or an experiment — none should be guessed.

| # | Unknown | Why it matters | How to settle it |
| --- | --- | --- | --- |
| ~~U-001~~ | ~~Does any CI workflow deploy on merge to `main`?~~ | — | **RESOLVED 2026-09-18.** Yes. `.github/workflows/cli-release.yml` publishes the CLI to npm via OIDC trusted publishing on push to `main`; `.github/workflows/docker.yml` pushes an image to ghcr on push to `main`. A merge to `main` is a release. Recorded in `project.md`; independently confirms DL-0002. |
| U-002 | Regulatory scope (if any) for this project. | Section 14 compliance. | Human confirmation only. |
| U-003 | Whether the published catalog is generated from this repo or authored elsewhere. | Determines whether a catalog change is in this repo's blast radius, and where `CatalogProvider` rows (ADR-0001) would be authored. | Trace `catalog-sync.ts` against the freellmapi.co publishing process; ask the maintainer. |
| U-004 | Is `cerebras` still a no-card free tier, and does `github` (GitHub Models) still exist? | Both are registered here as no-card free-key providers. A third-party September 2026 survey reports Cerebras moved to a card-backed trial and GitHub Models shut down. If the survey is right, two catalog entries are wrong and `free_type` for `cerebras` is `card_backed`, not `permanent`. | Live probe with a fresh account for each. Until then neither claim should be asserted; this is exactly what `last_verified_at = NULL` is for. |
| U-005 | Does Cohere's trial-key restriction make it unroutable for commercial users of this project? | Cohere documents that trial keys are limited to 1,000 calls/month and that production/commercial applications require a production key. The router currently treats a Cohere trial key as an ordinary candidate. | Read Cohere's current terms; decide whether `production_allowed = 'no'` plus the ADR-0001 `commercial_use` routing mode is sufficient mitigation, or whether the platform needs a louder warning at key-entry time. |
| U-006 | Which registered providers permit commercial use on the free tier at all? | `commercial_allowed` is the central new field in ADR-0001 and cannot be backfilled by inference. Known non-`yes` cases: Cohere (trial keys not for production), ElevenLabs (non-commercial + attribution, if ever added), Kilo (prompts logged for training — a different but related concern). | Per-provider terms review. Until reviewed, `unknown` is the honest value and the strict variant of the routing mode treats it as excluded. |
| U-007 | Do `:free` OpenRouter routes inherit upstream model licences in a way that makes provider-level `commercial_allowed` meaningless for that platform? | Decides whether OpenRouter's row is `model_dependent` with per-model overrides, or whether the whole field belongs on `models` (an ADR-0001 reversal condition). | Sample a dozen `:free` routes and compare their upstream licences. |
